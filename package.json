{
  "name": "bulk",
  "version": "0.1.0",
  "private": true,
  "description": "Bulk \u2014 single-user training app. Vanilla ES modules, zero runtime dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "python3 dev/serve.py 8123",
    "publish": "sh dev/publish.sh"
  },
  "engines": {
    "node": ">=18.13"
  }
}
